#include<stdio.h>
main()
{
int ret_val;
char sys_com[50];

strcpy(sys_com,"grep directory temp_file");
if((ret_val = system(sys_com))== 0)
{
	system(rcopy.sh `pwd`); 
}
else
{
	strcpy(sys_com,"grep executable temp_file");
	if((ret_val = system(sys_com))!= 0)
		strcpy(sys_com,"rcp cs3:/3u1/proj/$1/;
}

